{% extends "layout/_template.njk" %}

{% from "components/summary/_macro.njk" import onsSummary %}
{% from "components/button/_macro.njk" import onsButton %}
{% from "components/list/_macro.njk" import onsList %}
{% from "components/access-code/_macro.njk" import onsAccessCode %}
{% from "components/panel/_macro.njk" import onsPanel %}
{% from "components/details/_macro.njk" import onsDetails %}
{% from "components/breadcrumbs/_macro.njk" import onsBreadcrumbs %}
{% from "components/question/_macro.njk" import onsQuestion %}
{% from "components/radios/_macro.njk" import onsRadios %}
{% from "components/address-output/_macro.njk" import onsAddressOutput %}
{% from "components/input/_macro.njk" import onsInput %}
{% set pageConfig = { 
    "header": {
        "title": "Household assets survey",
        "signoutButton": {
        "text": "Save and sign out"}
        },
    "title": "Household asset survey",
    "footer": {
        "OGLLink": {
            "pre": 'All content is available under the',
            "link": 'Open Government Licence v3.0',
            "url": 'https://www.nationalarchives.gov.uk/doc/open-government-licence/version/3/',
            "post": ', except where otherwise stated'
        }
    }
} %}

{% block preMain %}
    {{
        onsBreadcrumbs({
            "ariaLabel": 'Back',
            "itemsList": [
                {
                    "url": 'example-address-input-non-editable',
                    "id": 'back',
                    "text": 'Back'
                }
            ]
        })
    }}
{% endblock %}
{% from "components/input/_macro.njk" import onsInput %}
{% from "components/question/_macro.njk" import onsQuestion %}

{% block main %}
<div class="ons-grid">
    <div class="ons-grid__col ons-col-8@m ">
      <div class="ons-pl-col">
    <div class="ons-page__body">
        <h1 class="ons-u-fs-xl">As a [Job title], what is the main activity you do?</h1>

        {% from "components/panel/_macro.njk" import onsPanel %}
        {{
            onsPanel({
                "body": '
                <p>To help us understand your job better, we would like to follow up with this question.</p>
                <p>An online advisor will be available in 2 minutes or they can contact you by [my preference].</p>
                '
            })
        }}
        <p>
            
        </p>
        <br>
        <div class="ons-btn-group">
                {{
                    onsButton({
                        "text": 'Continue'
                    })
                }}
                {{
                    onsButton({
                        "type": 'button',
                        "text": 'No thanks, I want to skip this question',
                        "name": 'email',
                        "variants": 'secondary'
                    })
                }}
            </div>
    </div>
    </div>
    </div>
    </div>
{% endblock %}

            